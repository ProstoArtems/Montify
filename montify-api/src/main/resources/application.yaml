spring:
  config:
    import: "optional:file:.env"

minio:
  endpoint: ${MINIO_ENDPOINT}
  access-key: ${MINIO_ACCESS_KEY}
  secret-key: ${MINIO_SECRET_KEY}
  bucket-name: ${MINIO_BUCKET_NAME}
  use-ssl: ${MINIO_USE_SSL}

app:
  redis:
    addr: ${REDIS_ADDR}
    queue-name: ${REDIS_QUEUE_NAME}